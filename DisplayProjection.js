'use strict'

function getDisplayPositions(currentPosition, heading, maxDistanceFilter,callback) {
	//get the poi within the filter distance

};

function 

DisplayProjection = {
	getDisplayPositions
};

module.exports = DisplayProjection;